---
description: Append a capture to the jotd inbox (append-only; the librarian organizes later)
---

Run `jotd add` with the user's text exactly as given — do not summarize, expand, or clean it
up; the inbox stores raw fragments:

!jotd add "$ARGUMENTS"

Confirm with the capture id it prints, in one short line. Do not organize anything now.
