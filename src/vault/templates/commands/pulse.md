---
description: Trigger a pulse run right now (budget rules still apply)
---

Run the pulse manually:

!vault pulse --now

Relay what it decided: nudges sent, what it suppressed and why (that reasoning is the
product — show it, don't hide it). If it errored, show the error and suggest
`vault status`.
