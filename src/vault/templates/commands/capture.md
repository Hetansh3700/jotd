---
description: Append a capture to the vault inbox (append-only; the librarian organizes later)
---

Run `vault add` with the user's text exactly as given — do not summarize, expand, or clean it
up; the inbox stores raw fragments:

!vault add "$ARGUMENTS"

Confirm with the capture id it prints, in one short line. Do not organize anything now.
